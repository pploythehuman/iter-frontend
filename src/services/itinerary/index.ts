import { apiGet, apiPost } from '../api';

const getItinerary = async (data: any) => {
  // const response = await apiPost<any, { message: string }>('user/register/', data);
  // return response;
};

export {
  getItinerary,
};